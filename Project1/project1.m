
csvfile = 'periodictabledata.csv';
%table = importdata(csvfile)
table = readtable(csvfile)
switch varagin
    case varargin == 0
    
    case varagin == 1
        
    case varargin == 2
        %tf = isa(
        %make another switch/if to determine is one of the variables is
        %char
end
varargin